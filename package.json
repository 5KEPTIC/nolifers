{
  "name": "nolifers",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "services/api-gateway",
    "packages/contracts"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @nolifers/web",
    "dev:gateway": "npm run dev --workspace @nolifers/api-gateway",
    "build:web": "npm run build --workspace @nolifers/web",
    "build:gateway": "npm run build --workspace @nolifers/api-gateway"
  }
}

